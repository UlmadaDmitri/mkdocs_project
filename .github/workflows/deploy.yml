name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master  # Здесь укажите имя вашей основной ветки

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8  # Укажите версию Python, если она необходима

      - name: Install dependencies
        run: |
          pip install mkdocs mkdocs-material  # Укажите необходимые зависимости

      - name: Build documentation
        run: |
          mkdocs build  # Команда для сборки документации

      - name: Add ssh key
        run: | 
          set -eu
          mkdir "/home/powerit/.ssh"
          echo "${{ secrets.ACTIONS_SSH_KEY }}" > "/home/powerit/.ssh/key"
          chmod 600 "/home/powerit/.ssh/key"

      - name: Update host content   
        shell: bash
        env:
          USER: ${{ secrets.ssh_user }}
          HOST: ${{ secrets.ssh_host }}
          DIRECTORY: ${{ secrets.directory }}
        run: |
          rsync -e "ssh -i /home/powerit/.ssh/key -o StrictHostKeyChecking=no $USER@$HOST " -avz /home/powerit/mkdocs_project/docs/*  $USER@$HOST:$DIRECTORY
